{
  "name": "genie",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm --filter @genie/core build && pnpm --filter @genie/plugin-example build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
